# local API (not for production)
DATABASE_URL=postgres://loop_kanban:change-me@127.0.0.1:5433/loop_kanban
PORT=3010
API_KEY=change-me-machine-key
SESSION_SECRET=change-me-session-hmac
REQUIRE_AUTH=1
CORS_ORIGINS=http://localhost:3000

# TypeSafe System One (Jev). Server-side only — never expose to the browser.
TYPESAFE_API_KEY=
# Optional: auto-apply field suggestions only when Choice confidence ≥ this (default 0.55)
# TYPESAFE_CONFIDENCE_FLOOR=0.55
# Optional: mark needsHumanReview when Noul ≥ this (default 0.55)
# TYPESAFE_REVIEW_NOUL=0.55
